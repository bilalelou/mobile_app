name: animegrab
description: Anime episode scraper and downloader CLI tool and Mobile App
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.0
  html: ^0.15.4
  dio: ^5.4.0
  path: ^1.9.0
  provider: ^6.1.2
  video_player: ^2.9.2
  chewie: ^1.8.5
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  test: ^1.25.0

flutter:
  uses-material-design: true
